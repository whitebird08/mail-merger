extends layout

block content  
 body
    .grandpa.grandpa-flex
      .grandpa-content
        .parent
          .item
            .mailMerge Mail Merge
          .item empty
        .parent
          .item Enter names / emails (one per line):
          .item Enter the text.  The following solutions are valid:
            .requirements
              ul
                li {first}
                li {last}
                li {email}
        .parent
          form(class='form' method="post")
            .item Contacts:
              .contacts
              textarea(id='contacts', name='contacts', type=text, placeholder='first, last, email@example.com')
            
            .item Subject
              .subject
                textarea(id='subject', name-'subject', type=text, placeholder='subject')
              .body Body
                textarea(id='message', name='message', type=text, placeholder='message')
                input(id ='preview', name='preview', type='submit', value='preview')
        //- .parent
        //-   .item empty
          //- .item
            //- .preview 
            
        .parent
          .recipientPreviews 
            .recipient
            input(id ='deleteEmail', name='deleteEmail', type='submit', value='x')
            textarea(id='contacts', name='contacts', type=text, placeholder='first last email@example.com')
            textarea(id='subject', name='subject', type=text, placeholder='subject')
            textarea(id='message', name='message', type=text, placeholder='message')
         

     